# Skill 系统融入落地方案

> 版本：1.0.0
> 日期：2025-02-26
> 状态：待实施

---

## 一、背景与问题分析

### 1.1 当前现状

项目已经实现了 Skill 系统的基础设施：

| 组件 | 状态 | 说明 |
|------|------|------|
| Skill 文件 | ✅ 已存在 | `.claude/skills/` 下有多个 skill 文件 |
| 后端 API | ✅ 已实现 | `/api/skills/context` 等接口 |
| 前端 Context | ✅ 已实现 | `SkillContext.tsx` 提供上下文管理 |
| API 参数传递 | ✅ 已实现 | `skillContext` 参数已添加到各 API |

### 1.2 核心问题

**当前实现只是"传递参数"，Skill 并没有真正发挥作用：**

```
当前流程：
用户输入 → 前端获取 skill 文本 → 传给后端 → 后端怎么用？不清楚 → 生成结果

问题：
1. 用户完全不知道 Skill 做了什么
2. Skill 内容没有被结构化利用
3. 和用户创作流程完全割裂
4. 没有真正帮助用户创作更好的音乐
```

### 1.3 用户痛点

| 痛点 | 具体表现 | Skill 应该解决 |
|------|----------|----------------|
| 不知道怎么描述 | "写一首好听的歌" - 太模糊 | 提供描述模板和引导 |
| 不懂音乐术语 | 不知道 BPM、风格、情绪怎么选 | 解释术语，提供知识 |
| 描述不完整 | 缺少关键信息，生成效果差 | 分析完整度，提示缺失信息 |
| 风格搭配困惑 | Pop 配什么情绪？ | 推荐风格组合，解释原因 |
| 不知道如何优化 | 生成结果不满意 | 诊断问题，给出改进建议 |

---

## 二、设计理念

### 2.1 核心理念

> **Skill 不应该是"隐形的后端参数"，而应该是"看得见的创作伙伴"**

### 2.2 设计原则

1. **可见性** - 用户能看到 Skill 如何帮助自己
2. **主动性** - Skill 主动提供建议，而不是等用户问
3. **个性化** - 根据用户输入提供针对性建议
4. **融合性** - 和创作流程紧密结合，不打断用户
5. **可操作性** - 建议可以直接应用，不是空话

### 2.3 三层融合架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    第一层：UI 融合层                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │ 创作助手面板 │  │ 智能提示   │  │ 引导式流程  │              │
│  └─────────────┘  └─────────────┘  └─────────────┘              │
├─────────────────────────────────────────────────────────────────┤
│                    第二层：业务融合层                            │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │ 描述分析    │  │ 风格推荐    │  │ 歌词辅助    │              │
│  └─────────────┘  └─────────────┘  └─────────────┘              │
├─────────────────────────────────────────────────────────────────┤
│                    第三层：AI 融合层                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │ Prompt 增强 │  │ 知识注入    │  │ 结果优化    │              │
│  └─────────────┘  └─────────────┘  └─────────────┘              │
└─────────────────────────────────────────────────────────────────┘
```

---

## 三、详细设计方案

### 3.1 第一层：UI 融合层

#### 3.1.1 创作助手面板

**位置**：创作页面右侧，可折叠

**功能**：
- 实时分析用户描述
- 显示完整度评分
- 提供改进建议
- 推荐风格组合
- 一键应用建议

**UI 设计**：

```
┌─────────────────────────────────────┐
│ 🎵 创作助手                 [收起] │
├─────────────────────────────────────┤
│                                     │
│ 📊 描述分析                         │
│ ┌─────────────────────────────────┐ │
│ │ 完整度: ████████░░ 80%          │ │
│ │                                 │ │
│ │ ✓ 主题: 夏天                    │ │
│ │ ✓ 情绪: 欢快                    │ │
│ │ ✓ 节奏: 明快                    │ │
│ │ ✓ 风格: Pop                     │ │
│ │ ✗ 乐器: 未描述                  │ │
│ │ ✗ 场景: 未描述                  │ │
│ └─────────────────────────────────┘ │
│                                     │
│ 💡 改进建议                         │
│ ┌─────────────────────────────────┐ │
│ │ • 添加乐器描述会更好             │ │
│ │   例如: 吉他、钢琴、电子合成器   │ │
│ │                                 │ │
│ │ • 描述使用场景可以获得更精准结果  │ │
│ │   例如: 适合海边、驾车、运动     │ │
│ └─────────────────────────────────┘ │
│                                     │
│ 🎯 风格推荐                         │
│ ┌─────────────────────────────────┐ │
│ │ Pop + Energetic ⭐ 推荐          │ │
│ │ 非常适合夏日主题，充满活力       │ │
│ │ [应用]                          │ │
│ │                                 │ │
│ │ Electronic + Relaxing           │ │
│ │ 夏日电子，轻松惬意               │ │
│ │ [应用]                          │ │
│ └─────────────────────────────────┘ │
│                                     │
│ 📝 优化后的描述                     │
│ ┌─────────────────────────────────┐ │
│ │ "一首欢快的夏日流行歌曲，节奏明  │ │
│ │  快，吉他伴奏，适合海边度假时聆  │ │
│ │  听，充满阳光和活力"             │ │
│ │                                 │ │
│ │ [应用此描述]                     │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

#### 3.1.2 智能输入提示

**位置**：描述输入框下方

**功能**：
- 实时分析用户输入
- 显示可能的补全建议
- 提供快捷标签

**UI 设计**：

```
┌─────────────────────────────────────────────────────┐
│ 描述你想要的音乐...                                  │
│ ┌─────────────────────────────────────────────────┐ │
│ │ 一首夏天的歌|                                    │ │
│ └─────────────────────────────────────────────────┘ │
│                                                     │
│ 💡 试试添加：                                       │
│ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐               │
│ │欢快  │ │放松  │ │浪漫  │ │激情  │               │
│ └──────┘ └──────┘ └──────┘ └──────┘               │
│ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐               │
│ │快节奏│ │中等  │ │慢节奏│ │律动感│               │
│ └──────┘ └──────┘ └──────┘ └──────┘               │
│ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐               │
│ │吉他  │ │钢琴  │ │电子  │ │弦乐  │               │
│ └──────┘ └──────┘ └──────┘ └──────┘               │
│                                                     │
│ 🎯 相似描述参考：                                   │
│ "一首轻快的夏日流行歌曲，节奏明快，吉他伴奏"        │
│ [使用此描述]                                        │
└─────────────────────────────────────────────────────┘
```

#### 3.1.3 引导式创作流程

**触发条件**：用户点击"新手引导"或首次使用

**功能**：
- 多步骤问答
- 自动生成描述
- 推荐设置

**UI 设计**：

```
┌─────────────────────────────────────────────────────┐
│           🎵 让我帮你完善音乐创意                    │
├─────────────────────────────────────────────────────┤
│                                                     │
│  步骤 1/5：这首歌是关于什么的？                     │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐    │
│  │夏天  │ │爱情  │ │友情  │ │回忆  │ │梦想  │    │
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘    │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐    │
│  │夜晚  │ │旅行  │ │成长  │ │自然  │ │自定义│    │
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘    │
│                                                     │
│  已选择：夏天 ✓                                     │
│                                                     │
│  ─────────────────────────────────────────────────  │
│                                                     │
│  步骤 2/5：想要什么情绪？                           │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐    │
│  │欢快  │ │放松  │ │浪漫  │ │悲伤  │ │激昂  │    │
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘    │
│                                                     │
│  已选择：欢快 ✓                                     │
│                                                     │
│  ─────────────────────────────────────────────────  │
│                                                     │
│  步骤 3/5：节奏快慢？                               │
│  ○ 快节奏 (130+ BPM) - 适合运动、派对              │
│  ● 中等节奏 (90-130 BPM) - 适合日常、驾车          │
│  ○ 慢节奏 (60-90 BPM) - 适合放松、睡眠             │
│                                                     │
│  ─────────────────────────────────────────────────  │
│                                                     │
│  步骤 4/5：主要乐器？                               │
│  ☑ 吉他    ☑ 钢琴    ☐ 电子合成器                  │
│  ☐ 弦乐    ☐ 打击乐  ☐ 管乐                        │
│                                                     │
│  ─────────────────────────────────────────────────  │
│                                                     │
│  步骤 5/5：使用场景？                               │
│  ☑ 海边    ☐ 驾车    ☐ 咖啡厅                      │
│  ☐ 运动    ☐ 派对    ☐ 睡前                        │
│                                                     │
├─────────────────────────────────────────────────────┤
│                                                     │
│  📝 根据你的选择，生成的描述：                      │
│  ┌─────────────────────────────────────────────────┐│
│  │ "一首欢快的夏日歌曲，中等节奏，吉他与钢琴伴奏，  ││
│  │   适合海边度假时聆听，充满阳光和活力"           ││
│  └─────────────────────────────────────────────────┘│
│                                                     │
│  🎯 推荐设置：                                      │
│  • 风格：Pop                                       │
│  • 情绪：Energetic                                 │
│  • 模型：chirp-v3-5                                │
│                                                     │
│  [上一步]                    [使用此描述并开始]    │
└─────────────────────────────────────────────────────┘
```

---

### 3.2 第二层：业务融合层

#### 3.2.1 描述分析服务

**功能**：分析用户描述的完整度，提供改进建议

**接口设计**：

```typescript
interface DescriptionAnalysis {
  // 完整度评分 (0-100)
  completeness: number;

  // 各维度分析
  dimensions: {
    theme: {
      found: boolean;
      value?: string;
      importance: 'required' | 'recommended' | 'optional';
    };
    mood: {
      found: boolean;
      value?: string;
      importance: 'required' | 'recommended' | 'optional';
    };
    rhythm: {
      found: boolean;
      value?: string;
      importance: 'required' | 'recommended' | 'optional';
    };
    instrument: {
      found: boolean;
      value?: string;
      importance: 'required' | 'recommended' | 'optional';
    };
    style: {
      found: boolean;
      value?: string;
      importance: 'required' | 'recommended' | 'optional';
    };
    scene: {
      found: boolean;
      value?: string;
      importance: 'required' | 'recommended' | 'optional';
    };
  };

  // 改进建议
  suggestions: Array<{
    type: 'missing' | 'improve' | 'add';
    dimension: string;
    message: string;
    examples?: string[];
  }>;

  // 推荐的风格组合
  recommendedStyles: Array<{
    genre: string;
    mood: string;
    reason: string;
    confidence: number;
  }>;

  // 优化后的描述
  optimizedDescription?: string;
}
```

**实现示例**：

```typescript
class DescriptionAnalyzer {
  // 关键词映射
  private themeKeywords = {
    '夏天': ['夏天', '夏日', 'sunshine', 'summer', '阳光'],
    '爱情': ['爱情', '恋爱', 'love', 'romantic', '浪漫'],
    '友情': ['友情', '朋友', 'friendship', 'friends'],
    '回忆': ['回忆', '过去', 'memory', 'nostalgia', '怀念'],
    // ...
  };

  private moodKeywords = {
    'energetic': ['欢快', '活力', 'energetic', 'upbeat', '激情'],
    'relaxing': ['放松', '轻松', 'relaxing', 'chill', '舒缓'],
    'romantic': ['浪漫', '温馨', 'romantic', 'love'],
    // ...
  };

  // 分析描述
  analyze(description: string): DescriptionAnalysis {
    const dimensions = this.detectDimensions(description);
    const completeness = this.calculateCompleteness(dimensions);
    const suggestions = this.generateSuggestions(dimensions);
    const recommendedStyles = this.recommendStyles(dimensions);
    const optimizedDescription = this.optimizeDescription(description, dimensions);

    return {
      completeness,
      dimensions,
      suggestions,
      recommendedStyles,
      optimizedDescription
    };
  }

  // 检测各维度
  private detectDimensions(description: string) {
    const lowerDesc = description.toLowerCase();

    return {
      theme: this.detectDimension(lowerDesc, this.themeKeywords),
      mood: this.detectDimension(lowerDesc, this.moodKeywords),
      rhythm: this.detectDimension(lowerDesc, this.rhythmKeywords),
      instrument: this.detectDimension(lowerDesc, this.instrumentKeywords),
      style: this.detectDimension(lowerDesc, this.styleKeywords),
      scene: this.detectDimension(lowerDesc, this.sceneKeywords)
    };
  }

  // 计算完整度
  private calculateCompleteness(dimensions: any): number {
    const weights = {
      theme: 25,      // 必填
      mood: 25,       // 必填
      rhythm: 15,     // 推荐
      instrument: 15, // 推荐
      style: 10,      // 可选
      scene: 10       // 可选
    };

    let score = 0;
    for (const [key, weight] of Object.entries(weights)) {
      if (dimensions[key]?.found) {
        score += weight;
      }
    }
    return score;
  }

  // 生成建议
  private generateSuggestions(dimensions: any): Suggestion[] {
    const suggestions: Suggestion[] = [];

    if (!dimensions.theme.found) {
      suggestions.push({
        type: 'missing',
        dimension: 'theme',
        message: '缺少歌曲主题',
        examples: ['夏天', '爱情', '友情', '回忆', '梦想']
      });
    }

    if (!dimensions.mood.found) {
      suggestions.push({
        type: 'missing',
        dimension: 'mood',
        message: '缺少情绪描述',
        examples: ['欢快', '放松', '浪漫', '激昂', '忧郁']
      });
    }

    // ... 更多建议逻辑

    return suggestions;
  }
}
```

#### 3.2.2 风格推荐服务

**功能**：基于用户描述和 Skill 知识，推荐最佳风格组合

**数据结构**：

```typescript
// 风格知识库 (来自 style-mixer skill)
interface StyleKnowledge {
  genres: Record<string, GenreInfo>;
  combinations: StyleCombination[];
  rules: RecommendationRule[];
}

interface GenreInfo {
  name: string;
  nameCn: string;
  description: string;
  characteristics: string[];
  bpmRange: [number, number];
  suitableFor: string[];
  compatibleMoods: string[];
  exampleDescription: string;
}

interface StyleCombination {
  genres: string[];
  moods: string[];
  description: string;
  useCases: string[];
  popularity: number; // 0-100
}

interface RecommendationRule {
  condition: {
    theme?: string[];
    scene?: string[];
    mood?: string[];
  };
  recommendation: {
    genres: string[];
    moods: string[];
    reason: string;
  };
  confidence: number;
}
```

**知识库示例**：

```typescript
const styleKnowledge: StyleKnowledge = {
  genres: {
    'pop': {
      name: 'Pop',
      nameCn: '流行',
      description: '流行音乐，旋律朗朗上口',
      characteristics: ['易于传唱', '结构简单', '旋律优美'],
      bpmRange: [90, 130],
      suitableFor: ['日常听歌', '聚会', '运动'],
      compatibleMoods: ['energetic', 'romantic', 'relaxing'],
      exampleDescription: '一首轻快的流行歌曲...'
    },
    'jazz': {
      name: 'Jazz',
      nameCn: '爵士',
      description: '爵士乐，即兴和复杂和弦',
      characteristics: ['即兴演奏', '复杂和弦', '摇摆节奏'],
      bpmRange: [60, 140],
      suitableFor: ['咖啡厅', '夜晚', '约会'],
      compatibleMoods: ['romantic', 'relaxing'],
      exampleDescription: '一首慵懒的爵士乐曲...'
    },
    // ... 更多风格
  },

  combinations: [
    {
      genres: ['pop', 'electronic'],
      moods: ['energetic'],
      description: '电子流行，适合派对和运动',
      useCases: ['运动', '派对', '驾车'],
      popularity: 85
    },
    {
      genres: ['jazz', 'classical'],
      moods: ['romantic', 'relaxing'],
      description: '优雅浪漫，适合约会和放松',
      useCases: ['约会', '晚餐', '放松'],
      popularity: 70
    },
    // ... 更多组合
  ],

  rules: [
    {
      condition: { theme: ['夏天', 'sunshine', 'summer'] },
      recommendation: {
        genres: ['pop', 'electronic'],
        moods: ['energetic', 'relaxing'],
        reason: '夏天的感觉，充满活力或轻松惬意'
      },
      confidence: 0.85
    },
    {
      condition: { theme: ['爱情', 'love', 'romantic'] },
      recommendation: {
        genres: ['pop', 'r&b', 'ballad'],
        moods: ['romantic', 'melancholic'],
        reason: '浪漫的情歌，表达爱意'
      },
      confidence: 0.90
    },
    // ... 更多规则
  ]
};
```

#### 3.2.3 歌词辅助服务

**功能**：基于 lyrics-wizard skill，提供歌词创作辅助

**功能列表**：

| 功能 | 说明 |
|------|------|
| 结构建议 | 根据风格推荐歌曲结构 |
| 韵脚提示 | 分析歌词，提示可用的韵脚 |
| 段落补全 | 帮助完成未完成的段落 |
| 风格适配 | 根据风格调整歌词风格 |

---

### 3.3 第三层：AI 融合层

#### 3.3.1 Prompt 增强服务

**功能**：将 Skill 知识融入 AI Prompt

**实现**：

```typescript
class PromptEnhancer {
  // 增强描述 Prompt
  enhanceDescriptionPrompt(userPrompt: string, skillContext: string): string {
    return `你是一位专业的音乐创作顾问。根据以下专业知识，帮助用户优化音乐描述。

## 专业知识
${skillContext}

## 用户原始描述
${userPrompt}

## 任务
1. 分析用户描述的完整度
2. 识别缺失的关键信息
3. 生成一个更完整、更专业的描述
4. 保持用户的原始意图

## 输出格式
{
  "analysis": {
    "theme": "检测到的主题",
    "mood": "检测到的情绪",
    "missingElements": ["缺失元素1", "缺失元素2"]
  },
  "enhancedDescription": "优化后的描述",
  "suggestions": ["建议1", "建议2"]
}
`;
  }

  // 增强歌词生成 Prompt
  enhanceLyricsPrompt(idea: string, style: string, skillContext: string): string {
    return `你是一位专业的歌词创作专家。根据以下专业知识创作歌词。

## 专业知识
${skillContext}

## 创作要求
- 主题: ${idea}
- 风格: ${style}

## 创作原则
1. 歌词要朗朗上口，易于演唱
2. 注意押韵的自然性
3. 情感表达要真挚
4. 根据曲风调整歌词的节奏感
5. 副歌部分要突出记忆点

## 歌词结构
[Intro] - 前奏
[Verse 1] - 主歌第一段
[Pre-Chorus] - 导歌
[Chorus] - 副歌（高潮）
[Verse 2] - 主歌第二段
[Chorus] - 副歌
[Bridge] - 桥段
[Chorus] - 最后副歌
[Outro] - 尾奏

请创作完整的歌词：
`;
  }
}
```

#### 3.3.2 知识注入服务

**功能**：将 Skill 知识注入到生成过程中

**实现**：

```typescript
class KnowledgeInjector {
  // 注入风格知识
  injectStyleKnowledge(tags: string[], knowledge: StyleKnowledge): string {
    const relatedGenres = tags.map(tag => knowledge.genres[tag]).filter(Boolean);

    if (relatedGenres.length === 0) return '';

    return `
## 风格知识
${relatedGenres.map(g => `
### ${g.nameCn} (${g.name})
- 特点: ${g.characteristics.join(', ')}
- BPM范围: ${g.bpmRange[0]}-${g.bpmRange[1]}
- 适合场景: ${g.suitableFor.join(', ')}
`).join('\n')}
`;
  }

  // 注入创作指南
  injectCreationGuide(mode: string, skillContext: string): string {
    return `
## 创作指南
${skillContext}

## 当前模式: ${mode}
${mode === 'inspiration' ? '灵感模式：AI 自动生成歌词和风格' : ''}
${mode === 'custom' ? '自定义模式：用户控制歌词和风格' : ''}
`;
  }
}
```

---

## 四、Skill 文件重构

### 4.1 结构化 Skill 格式

为了让 Skill 更易于程序化利用，需要重构 Skill 文件格式：

**当前格式**：纯 Markdown 文本

**新格式**：结构化 YAML + Markdown

```yaml
---
name: music-create
description: 专业音乐创作助手
version: 2.0
---

# 元数据 (机器可读)
---
skill:
  id: music-create
  type: creation
  pages: [create, quick-create]
  triggers:
    - user_input
    - page_load
    - request_help
---

# 描述要素分析规则 (机器可读)
---
analysis:
  dimensions:
    theme:
      weight: 25
      required: true
      keywords:
        zh: [夏天, 爱情, 友情, 回忆, 梦想, 旅行, 成长]
        en: [summer, love, friendship, memory, dream, travel, growth]
      extract_regex: "(关于|描写|讲述)\\s*(.+?)\\s*(的|故事|歌)"

    mood:
      weight: 25
      required: true
      keywords:
        zh: [欢快, 放松, 浪漫, 悲伤, 激昂, 忧郁, 温暖]
        en: [happy, relaxing, romantic, sad, energetic, melancholic, warm]

    rhythm:
      weight: 15
      required: false
      keywords:
        zh: [快节奏, 慢节奏, 律动感, 轻快, 悠扬]
        en: [fast, slow, groovy, light, melodious]
      map_to:
        fast: { bpm: [130, 180] }
        medium: { bpm: [90, 130] }
        slow: { bpm: [60, 90] }

    instrument:
      weight: 15
      required: false
      keywords:
        zh: [吉他, 钢琴, 电子, 弦乐, 打击乐, 管乐]
        en: [guitar, piano, electronic, strings, percussion, brass]

    style:
      weight: 10
      required: false
      keywords:
        zh: [流行, 摇滚, 爵士, 电子, 民谣, 说唱]
        en: [pop, rock, jazz, electronic, folk, hip-hop]

    scene:
      weight: 10
      required: false
      keywords:
        zh: [海边, 驾车, 运动, 睡前, 咖啡厅, 派对]
        en: [beach, driving, workout, sleep, cafe, party]
---

# 风格推荐规则 (机器可读)
---
recommendations:
  - when:
      theme: [夏天, summer, sunshine]
    suggest:
      genres: [pop, electronic]
      moods: [energetic, relaxing]
      reason: "夏天的感觉，充满活力或轻松惬意"
      confidence: 0.85

  - when:
      theme: [爱情, love, romantic]
    suggest:
      genres: [pop, r&b, ballad]
      moods: [romantic, melancholic]
      reason: "浪漫的情歌，表达爱意"
      confidence: 0.90

  - when:
      scene: [运动, workout, gym]
    suggest:
      genres: [electronic, hip-hop, rock]
      moods: [energetic, intense]
      reason: "适合运动的节奏，激励人心"
      confidence: 0.88
---

# 描述模板 (机器可读)
---
templates:
  complete: "{mood}的{theme}{style}歌曲，{rhythm}，{instrument}伴奏，{scene}"

  minimal: "{theme}的{style}"

  examples:
    - "一首欢快的夏日流行歌曲，节奏明快，吉他伴奏，适合海边度假"
    - "一首浪漫的爱情民谣，轻柔的钢琴旋律，适合夜晚独自聆听"
    - "一首充满力量的摇滚歌曲，快节奏，电吉他主导，适合运动时听"
---

# 用户指南 (人类可读)

## 创作模式

### 🌟 灵感模式
最简单的创作方式，只需要一句话描述...

### ✏️ 自定义模式
用户完全控制歌词和风格...

## 创作技巧

1. **描述要具体** - "一首关于夏天的歌" 比 "一首歌" 效果更好
2. **添加情绪词** - 描述你想要的感觉
3. **指定节奏** - 快、慢、中等
4. **选择乐器** - 吉他、钢琴、电子等
```

### 4.2 Skill 解析服务

**功能**：解析结构化 Skill 文件

```typescript
interface StructuredSkill {
  metadata: {
    id: string;
    name: string;
    type: string;
    version: string;
    pages: string[];
    triggers: string[];
  };

  analysis: {
    dimensions: Record<string, DimensionConfig>;
  };

  recommendations: RecommendationRule[];

  templates: {
    complete: string;
    minimal: string;
    examples: string[];
  };

  content: string; // 原始 Markdown 内容
}

class SkillParser {
  parse(markdown: string): StructuredSkill {
    // 1. 提取 YAML frontmatter
    const frontmatter = this.extractYamlFrontmatter(markdown);

    // 2. 提取嵌入式 YAML 块
    const skillConfig = this.extractYamlBlocks(markdown);

    // 3. 合并配置
    return {
      metadata: frontmatter,
      analysis: skillConfig.analysis?.dimensions || {},
      recommendations: skillConfig.recommendations || [],
      templates: skillConfig.templates || {},
      content: this.removeYamlBlocks(markdown)
    };
  }
}
```

---

## 五、实施计划

### 5.1 阶段划分

```
Phase 1: 基础设施 (1-2 周)
├── 重构 Skill 文件格式
├── 实现 Skill 解析服务
├── 创建描述分析服务
└── 实现风格推荐服务

Phase 2: UI 组件 (2-3 周)
├── 创建创作助手面板组件
├── 实现智能输入提示
├── 创建引导式创作流程
└── 集成到创作页面

Phase 3: AI 增强 (1-2 周)
├── 实现 Prompt 增强服务
├── 集成知识注入
├── 优化后端 AI 调用
└── 添加结果分析

Phase 4: 测试优化 (1 周)
├── 用户测试
├── 收集反馈
├── 优化体验
└── 性能调优
```

### 5.2 Phase 1 详细任务

#### 任务 1.1：重构 Skill 文件

**目标**：将现有 Skill 文件重构为结构化格式

**文件列表**：
- `.claude/skills/music-create/SKILL.md`
- `.claude/skills/lyrics-wizard/SKILL.md`
- `.claude/skills/style-mixer/SKILL.md`
- `.claude/skills/prompt-engineer/SKILL.md`

**工作量**：每个文件约 2-3 小时

#### 任务 1.2：实现 Skill 解析服务

**目标**：创建能够解析结构化 Skill 的服务

**文件**：
- `frontend/src/services/skill/SkillParser.ts`
- `frontend/src/services/skill/types.ts`

**工作量**：约 1 天

#### 任务 1.3：创建描述分析服务

**目标**：实现描述完整度分析

**文件**：
- `frontend/src/services/skill/DescriptionAnalyzer.ts`
- `frontend/src/services/skill/DescriptionAnalyzer.test.ts`

**工作量**：约 2 天

#### 任务 1.4：实现风格推荐服务

**目标**：基于 Skill 知识推荐风格

**文件**：
- `frontend/src/services/skill/StyleRecommender.ts`
- `frontend/src/services/skill/styleKnowledge.ts`

**工作量**：约 2 天

### 5.3 Phase 2 详细任务

#### 任务 2.1：创建创作助手面板

**目标**：实现右侧助手面板

**文件**：
- `frontend/src/components/SkillAssistantPanel.tsx`
- `frontend/src/components/SkillAssistantPanel.styles.ts`

**工作量**：约 2 天

#### 任务 2.2：实现智能输入提示

**目标**：在输入框添加实时提示

**文件**：
- `frontend/src/components/SmartInput.tsx`
- `frontend/src/components/SmartInput.styles.ts`

**工作量**：约 1 天

#### 任务 2.3：创建引导式创作流程

**目标**：实现多步骤引导

**文件**：
- `frontend/src/components/GuidedCreation.tsx`
- `frontend/src/components/GuidedCreation.styles.ts`

**工作量**：约 2 天

#### 任务 2.4：集成到创作页面

**目标**：将组件集成到 CreateView

**文件**：
- `frontend/src/views/CreateView.tsx`

**工作量**：约 1 天

---

## 六、技术架构

### 6.1 目录结构

```
frontend/src/
├── services/
│   └── skill/
│       ├── index.ts                    # 导出入口
│       ├── types.ts                    # 类型定义
│       ├── SkillParser.ts              # Skill 解析器
│       ├── DescriptionAnalyzer.ts      # 描述分析器
│       ├── StyleRecommender.ts         # 风格推荐器
│       ├── PromptEnhancer.ts           # Prompt 增强器
│       └── knowledge/
│           ├── styleKnowledge.ts       # 风格知识库
│           └── lyricsKnowledge.ts      # 歌词知识库
│
├── components/
│   └── skill/
│       ├── SkillAssistantPanel.tsx     # 助手面板
│       ├── SmartInput.tsx              # 智能输入
│       ├── GuidedCreation.tsx          # 引导创作
│       ├── DescriptionAnalysis.tsx     # 描述分析显示
│       ├── StyleRecommendation.tsx     # 风格推荐显示
│       └── QuickTags.tsx               # 快捷标签
│
├── hooks/
│   └── useSkillAssistant.ts            # Skill 助手 Hook
│
└── contexts/
    └── SkillContext.tsx                # 更新 Context
```

### 6.2 数据流

```
用户输入
    │
    ▼
┌─────────────────┐
│  SmartInput     │ ◄── 实时提示
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ DescriptionAnalyzer │ ◄── Skill 知识
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│SkillAssistantPanel │ ◄── 显示分析结果
└────────┬────────┘
         │
         ├──► StyleRecommender ──► 风格推荐
         │
         ├──► PromptEnhancer ──► 增强后的 Prompt
         │
         └──► API 调用 ──► 后端
```

### 6.3 关键接口

```typescript
// Skill 助手服务接口
interface ISkillAssistantService {
  // 解析 Skill
  parseSkill(skillContent: string): StructuredSkill;

  // 分析描述
  analyzeDescription(description: string): DescriptionAnalysis;

  // 推荐风格
  recommendStyles(analysis: DescriptionAnalysis): StyleRecommendation[];

  // 增强 Prompt
  enhancePrompt(prompt: string, context: SkillContext): string;

  // 生成优化描述
  generateOptimizedDescription(
    original: string,
    analysis: DescriptionAnalysis
  ): string;
}

// Hook 接口
interface UseSkillAssistantResult {
  // 状态
  analysis: DescriptionAnalysis | null;
  recommendations: StyleRecommendation[];
  isAnalyzing: boolean;

  // 方法
  analyze: (description: string) => void;
  applyRecommendation: (rec: StyleRecommendation) => void;
  applyOptimizedDescription: () => void;
  startGuidedCreation: () => void;
}
```

---

## 七、验收标准

### 7.1 功能验收

| 功能 | 验收标准 |
|------|----------|
| 描述分析 | 能正确识别主题、情绪、节奏等维度 |
| 完整度评分 | 评分准确反映描述质量 |
| 改进建议 | 建议具体可操作 |
| 风格推荐 | 推荐与描述相关，解释合理 |
| 一键应用 | 建议可一键应用到表单 |
| 引导流程 | 能完整引导用户完成描述 |

### 7.2 体验验收

| 指标 | 目标值 |
|------|--------|
| 分析响应时间 | < 500ms |
| 首屏加载 | 不增加额外加载时间 |
| 用户满意度 | > 80% |
| 描述完整度提升 | 平均提升 30% |

### 7.3 技术验收

| 项目 | 标准 |
|------|------|
| 代码覆盖率 | > 80% |
| TypeScript | 无 any 类型 |
| 组件复用 | 核心组件可复用 |
| 性能 | 无明显性能回归 |

---

## 八、风险与应对

### 8.1 技术风险

| 风险 | 可能性 | 影响 | 应对措施 |
|------|--------|------|----------|
| Skill 解析复杂 | 中 | 中 | 先实现简单版本，逐步增强 |
| 分析准确性不足 | 中 | 高 | 使用混合策略：规则 + AI |
| 性能问题 | 低 | 中 | 添加防抖、缓存 |
| 后端不配合 | 低 | 高 | 前端独立实现核心功能 |

### 8.2 产品风险

| 风险 | 可能性 | 影响 | 应对措施 |
|------|--------|------|----------|
| 用户不理解功能 | 中 | 中 | 添加引导说明 |
| 建议不准确 | 中 | 高 | 允许用户忽略建议 |
| 打断创作流程 | 低 | 高 | 设计为可选、可折叠 |

---

## 九、后续规划

### 9.1 短期优化 (1-2 月)

- 基于用户反馈优化推荐算法
- 添加更多风格知识
- 支持更多语言

### 9.2 中期扩展 (3-6 月)

- 对话式创作助手
- 个性化推荐
- 学习用户偏好

### 9.3 长期愿景 (6-12 月)

- 完整的音乐创作 AI 顾问
- 跨平台支持
- 社区知识共享

---

## 附录

### A. 参考文档

- [Claude Code Skills 文档](./Claude%20code%20skills文档.md)
- [Suno API 文档](./sunoApi.md)

### B. 相关代码

- `frontend/src/contexts/SkillContext.tsx`
- `frontend/src/hooks/useSkillEnhancedAI.ts`
- `frontend/src/services/api.ts`
- `.claude/skills/music-create/SKILL.md`

### C. 术语表

| 术语 | 说明 |
|------|------|
| Skill | Claude Code 的技能扩展机制 |
| Context | 技能的上下文信息 |
| Dimension | 描述分析的维度 (主题、情绪等) |
| Recommendation | 风格推荐 |
| Guided Creation | 引导式创作流程 |

---

**文档版本历史**

| 版本 | 日期 | 作者 | 变更说明 |
|------|------|------|----------|
| 1.0.0 | 2025-02-26 | Claude | 初始版本 |
